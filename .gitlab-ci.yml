stages:
  - build
  - test

dummy_test_to_trigger_pulpino_ci:
  stage: test
  script:
    - ci/dummy.csh

trigger_build:
  stage: deploy
  script:
    - "curl -X POST -F token=TOKEN -F ref=REF_NAME https://iis-git.ee.ethz.ch/api/v4/projects/72/trigger/pipeline"
